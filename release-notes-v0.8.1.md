# Tarkov Server Guard v0.8.1

상태 안내와 여러 항목 선택 흐름을 다듬은 안정성 업데이트입니다. 기존 메모·차단 정보·백업 형식·지역 DB는 그대로 유지됩니다.

## 변경 사항

- `로그 없음` 도움말에 게임 버그로 필요한 로그가 기록되지 않는 경우를 함께 안내합니다.
- 서버 차단 완료 뒤 메인 화면의 두 줄 상태 안내가 작은 창과 Windows 화면 배율에서도 잘리지 않도록 개선했습니다.
- 메모보관함에서 전체 선택한 첫 행의 체크 표시가 포커스를 옮기지 않아도 즉시 갱신되도록 수정했습니다.
- 서버차단현황에서 선택 열 헤더와 `Ctrl+A`로 전체 선택하고 현재 선택 개수를 확인할 수 있습니다.
- 선택 열과 일괄 작업 버튼의 접근성 설명을 정리하고 현재 선택 개수를 화면 읽기 프로그램에도 제공합니다.
- 메모보관함·서버차단현황의 정보 헤더와 메인 화면 `차단`·`해제` 열에 오름차순·내림차순·기본 순서 정렬과 주황색 방향 표시를 추가했습니다. 메인 정렬 헤더에는 동작 안내를 제공하고, 두 보조 창의 선택 헤더는 현재 전체 선택 상태를 보여 주는 체크박스로 개선했습니다.
- 차단 목록 백업 기본 파일명에 날짜와 시간을 포함하고, 완료 안내에는 실제 저장된 파일명만 표시합니다.
- 새 레이드 로그는 경로를 다시 적용하지 않고 메인 화면의 `조회`로 다시 읽는다는 안내를 문제 해결 문서에 반영했습니다.

## 다운로드

- **일반 사용자 권장:** `SpiritSchema.TarkovServerGuard-win-Setup.exe`
- **설치 없이 사용:** `SpiritSchema.TarkovServerGuard-win-Portable.zip`

v0.8.0 사용자는 앱의 업데이트 안내 또는 `업데이트확인`을 통해 v0.8.1을 적용할 수 있습니다. Portable은 압축을 푼 뒤 폴더 안의 파일 구성을 유지해 주세요.

자세한 기능·사용법·개인정보·라이선스 안내는 [README](https://github.com/Spirit-Schema/tarkov-server-guard#readme)에서 확인할 수 있습니다.
