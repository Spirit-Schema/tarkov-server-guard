# Tarkov Server Guard v0.8.2

메모 이동성과 경로 설정 화면을 다듬은 업데이트입니다. v0.8.1 검수 과정에서 반영한 개선과 기존 메모·차단 정보·지역 DB도 그대로 유지됩니다.

## 변경 사항

- TSG 경로의 `직접선택`, `자동 찾기`, `적용` 버튼 문구가 아래쪽으로 치우쳐 보이지 않도록 세로 위치를 중단에 맞췄습니다.
- 메모보관함에서 일반 레이드 메모와 유저신고 메모를 하나의 JSON 파일로 백업하고, 기존 항목을 덮어쓰지 않은 채 없는 메모만 복원할 수 있습니다.
- 연결된 스크린샷 원본 파일은 포함하지 않고 검증된 로컬 이미지 경로만 백업·복원합니다. 경로에는 Windows 사용자명 같은 개인정보가 포함될 수 있으며, 다른 PC의 같은 경로에 원본이 없으면 스크린샷은 열리지 않습니다.
- 메모보관함의 `맵 · 게임유형` 열에 EFT 레이드 유형 또는 Arena 게임 모드를 함께 표시하고, 처음 열 때 가로 스크롤이 생기지 않도록 기본 창 폭을 넓혔습니다.

## 다운로드

- **일반 사용자 권장:** `SpiritSchema.TarkovServerGuard-win-Setup.exe`
- **설치 없이 사용:** `SpiritSchema.TarkovServerGuard-win-Portable.zip`

현재 공개된 v0.8.0 사용자는 앱의 업데이트 안내 또는 `업데이트확인`을 통해 v0.8.2를 적용할 수 있습니다. Portable은 압축을 푼 뒤 폴더 안의 파일 구성을 유지해 주세요.

자세한 기능·사용법·개인정보·라이선스 안내는 [README](https://github.com/Spirit-Schema/tarkov-server-guard#readme)에서 확인할 수 있습니다.
