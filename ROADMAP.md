# 로드맵

이 문서는 Tarkov Server Guard의 예정 작업을 목표 시기별로 정리합니다. 같은 시기 안의 나열 순서는 구현 우선순위를 뜻하지 않으며, 세부 범위는 개발·검증 결과에 따라 조정될 수 있습니다.

## 의사결정 상태 표기 원칙

- 각 작업은 `RM-001` 형식의 고유 식별번호를 사용합니다. 버전·상태·제목이 바뀌어도 기존 번호를 유지하고, 삭제·취소된 번호를 다른 작업에 재사용하지 않습니다.
- 소통과 작업 기록에서는 가능하면 제목과 함께 식별번호를 사용합니다. 예: `RM-008 앱 시작 시 차단 중 서버 즉시 표시`.
- `[확정]`: 사용자가 범위와 시기를 결정한 작업입니다. 실제 작업 지시로 해석합니다.
- `[검토 필요]`: 필요성·범위·시기가 아직 결정되지 않은 후보입니다. 검토 없이 구현하지 않습니다.
- `[보류]`: 현재 제품 범위에서는 진행하지 않기로 한 항목입니다. 기존 시험 구현이 있어도 정식 코드·빌드·배포 과정에 연결하지 않습니다.
- `[제안]`: 구현 가능성을 설명하기 위한 참고안입니다. 사용자 결정이 아니며 작업 지시로 해석하지 않습니다.
- 앞으로 로드맵에 구현 방법을 추가할 때는 `사용자 결정`과 `구현 제안(미확정)`을 분리해 기록합니다. 사용자가 승인하지 않은 구현 방법을 `[확정]`으로 바꾸지 않습니다.

## 현재 배포 운영 기준

### RM-001 · [확정] v1.0 이전 공식 배포 범위

**사용자 결정**

- 현재 공개 정식본의 기능 범위로 충분하며, 시험 버전에만 구현된 복잡한 공식 배포본 자체 검증 기능은 정식본에 추가하지 않음
- GitHub Releases를 공식 배포처로 사용하고 릴리스 자산의 SHA-256을 계속 공개
- 공개 자산 재다운로드 검증과 최종 배포물 전용 민감정보·압축 안전 검사는 현재 필수 배포 작업에서 제외

**운영 메모**

- 이 결정은 이미 공개된 릴리스 자산을 다시 만들거나 교체하라는 지시가 아님
- 코드 서명 도입 전까지 SHA-256은 파일 동일성 확인을 위한 보조 수단이며 공식 게시자 신원 보증 수단으로 표현하지 않음

## v0.8.0 필수

### RM-002 · [확정] 실게임 측정값 헤더 열 조정

- `실게임 RTT`와 `실게임 패킷손실` 헤더를 좌측 정렬
- 현재 지정된 열 최소 축소 폭을 제거해 사용자가 더 좁게 줄일 수 있도록 변경
- 열 폭과 관계없이 헤더 문구는 `실게임`과 `RTT` 또는 `패킷손실`의 정확한 두 줄 구성을 유지하고 세 줄로 갈라지지 않게 표시

### RM-003 · [확정] 메모보관함 헤더 디자인 통일

- 메모보관함 헤더의 테두리 디자인만 서버차단현황 헤더의 굵기·색상·경계 처리에 맞춰 통일
- 서버차단현황과 같은 좌측 `선택` 열과 `선택 삭제` 기능을 제공해 여러 일반 레이드 메모·유저신고 메모를 한 번에 정리
- 메인 접속 기록 표의 헤더 디자인은 변경하지 않음

### RM-004 · [확정] 앱 종료 후에도 유지되는 차단 동작 안내 강화

- `서버차단현황` 상단에 창을 닫으면 TSG가 완전히 종료되어 백그라운드에 상주하지 않고 TSG 프로세스의 추가 CPU·메모리·네트워크 사용이 없다는 점을 표시
- 차단 리스트는 Windows 방화벽에 저장되므로 해당 앱 종료·PC 재부팅 뒤에도 앱에서 해제할 때까지 적용된다는 점을 차단 완료 상태와 도움말에 함께 표시
- 메인 화면을 복잡하게 만들거나 실행할 때마다 별도 팝업을 띄우지 않고, 사용자가 차단 상태를 확인하거나 차단을 완료한 시점에 안내
- 프로그램 안의 안내와 회귀 테스트를 우선 반영하고, 정식 게시 시 GitHub 릴리스 안내에도 같은 동작을 오해 없이 요약

### RM-005 · [확정] 입장시간 계산 정확도 개선

- 현재 `GameStarted 로그 시각 - 서버 IP 감지 시각` 계산은 서버 배정 이전의 일부 입장 과정을 제외해 실제 체감보다 짧게 표시될 수 있음
- `GameStarted` 로그의 `real` 측정값을 우선 사용하고, `real`이 없으면 기본 측정값, 둘 다 없거나 유효하지 않으면 기존 시각 차이 계산으로 대체
- 서로 다른 레이드의 이벤트가 연결되지 않도록 다음 서버 배정 경계와 최대 허용시간을 검증하고 EFT·Arena·재접속·동일 폴더 다중 레이드 테스트 추가

### RM-007 · [확정] `맵 · 게임유형` PvP 시즌 번호 표시

- TSG 접속 기록의 `맵 · 게임유형` 열에서 신뢰할 수 있는 시즌 번호가 있으면 `PvP시즌1`, `PvP시즌2`처럼 공백 없이 숫자만 붙여 표시
- KORD BREACH 같은 시즌명·내부 키는 판정용 별도 매핑에만 보관하고 화면 표기에 섞지 않아, 이후 시즌 이름이 달라져도 열의 표시 형식을 유지
- 로그에 명시된 유효한 시즌 번호를 최우선으로 사용하고, 번호가 없을 때만 검증된 클라이언트 버전 매핑을 보조 자료로 사용
- 검증되지 않은 미래 버전이나 시즌 정보가 없는 기록은 현재 시즌 번호로 추측하지 않고 기존 `PvP시즌`으로 안전하게 표시
- 각 레이드에 판정한 번호와 근거를 보관해 과거 기록을 현재 시즌으로 덮어쓰지 않으며 복사·캐시·병합에서도 유지
- EFT 시즌 PvP, EFT 일반 PvP, EFT PvE, Arena가 서로 섞이지 않는지와 명시 번호 우선·알 수 없는 미래 시즌 fallback을 회귀 테스트

### RM-008 · [확정] 앱 시작 시 차단 중 서버 즉시 표시

- 앱이 레이드 로그를 불러오면 사용자가 `조회`를 누르기 전에도 현재 목록의 고유 서버 IP에 대한 앱 관리 Windows 방화벽 상태를 읽기 전용으로 한 번 확인
- 차단 중인 서버는 접속 기록의 `현재 핑` 열과 선택 상세에 `차단 중`으로 우선 표시하고, 확인 실패 시 기존 `조회 전` 표시와 수동 조회 기능을 그대로 유지
- 초기 확인은 UAC·방화벽 변경·핑·지역 조회를 실행하지 않으며, 새 로그 로드나 앱 종료로 오래된 결과가 현재 목록에 섞이지 않도록 취소·세대·대상 교집합을 검증

### RM-009 · [확정] 업데이트 완료 뒤 패치 내역 1회 안내

- 메인 화면과 업데이트 가능 안내창의 상시 `패치내역`·`변경 사항` 버튼은 제공하지 않음
- Velopack의 실제 `OnAfterUpdateFastCallback`에서 완료된 버전을 기록하고, 업데이트된 설치형 정식 빌드의 다음 첫 실행에서 해당 버전 내장 변경 사항을 정확히 한 번 표시
- 신규 설치·Portable·개발·데모·화면 미리보기에서는 자동 표시하지 않고 실패·취소된 업데이트도 완료로 기록하지 않음
- 완료 내역은 앱에 포함된 길이 제한 읽기 전용 텍스트를 사용해 오프라인에서도 표시하며, 별도 GitHub 조회나 외부 HTML·스크립트를 실행하지 않음
- 여러 프로세스가 동시에 시작해도 pending marker를 원자적으로 하나만 선점하고, 소비 상태를 먼저 저장한 뒤 표시하는 at-most-once 정책을 사용. 상태 저장 실패 시 반복 팝업보다 안내 유실을 선택해 표시하지 않음
- 완료 상태 처리와 대화상자 구조는 격리된 단위 테스트로 검증하고, 실제 표시 흐름은 설치형 업데이트 완료 뒤의 다음 첫 실행에서 최종 확인. 임의 미리보기 인자는 제공하지 않음

### RM-010 · [확정] 차단 목록 백업·복원

- 포맷이나 PC 교체 전에 기존 차단 목록을 파일로 저장하고 새 환경에서 불러올 수 있는 `내보내기`·`불러오기` 기능 제공
- 내보내기는 Windows 방화벽에서 현재 앱이 실제로 관리하는 차단 규칙을 기준으로 하고, 해당 IP의 데이터센터·추정 지역·차단 시각·사용자 메모를 결합한 버전형 UTF-8 JSON 파일로 저장
- 내보내기 파일에는 계정정보·SID·로그 경로·기기 식별정보 등 복원에 필요하지 않은 개인정보와 로컬 정보는 포함하지 않음
- 불러오기는 파일 버전·크기·항목 수를 제한하고, 각 주소를 정확한 공인 IPv4로 재검증하며 중복·이미 차단됨·잘못된 항목을 구분
- 적용 전에 `새로 차단`, `이미 차단됨`, `적용 제외` 목록과 메모 포함 여부를 미리 보여 주고 사용자가 확인한 항목만 적용
- 기존 차단의 로컬 메모와 지역정보는 기본적으로 덮어쓰지 않고, 새 PC처럼 기존 정보가 없는 항목에만 백업 정보를 복원
- 기존 일괄 방화벽 처리 경로를 차단 추가에도 확장해 여러 IP를 한 번의 관리자 권한 요청으로 적용하고, 부분 실패 시 IP별 결과를 표시
- 방화벽 차단이 성공했거나 이미 앱 관리 규칙으로 차단된 IP에 대해서만 부가정보를 저장한 뒤 실제 방화벽 상태를 다시 조회해 최종 결과 검증

## v0.8.0 이후 근시일 검토

아래 항목은 `v0.8.1`부터 `v0.x` 사이의 후속 버전 후보이며, `v1.0.0`보다 먼저 검토·추진합니다. 사용자 수요와 구현·검증 범위에 따라 개별 릴리스 배정은 달라질 수 있습니다.

### RM-011 · [보류] 사용자 선택형 설치 제거 UI

- Velopack 설치본은 기존 버전을 포함해 Windows 제어판·`설치된 앱`에서 제거할 수 있으므로 별도 앱 버튼·시작 인자·시작 메뉴 제거 바로가기는 현재 제품에 적용하지 않음
- 사용자 데이터와 앱 관리 방화벽 규칙의 선택 정리까지 포함한 기존 구현은 삭제하지 않고 [`deferred/uninstall/README.md`](deferred/uninstall/README.md)의 소스·테스트·의존성·한계·재도입 체크리스트로 격리 보존
- 실제 사용자 요구가 확인되기 전에는 컴파일·패키징·Velopack runtime hook에 연결하지 않으며, 필요 시 제어판 제거와 별도 `로컬 데이터 정리`·`방화벽 규칙 전체 해제` 기능 중 더 단순한 흐름부터 재검토

### RM-012 · [검토 필요] 지역 필터의 최근 100개 범위

- 현재 방식인 `전체 최신 100개에서 선택 지역만 표시`는 모든 지역이 같은 최신 기록 범위를 공유해 빠르고 비교하기 쉬움
- 대안인 `선택 지역 기록 중 최신 100개 표시`는 이용 빈도가 낮은 지역도 충분한 기록을 보여 주지만, 더 오래된 기록과 추가 로그 탐색이 포함될 수 있음
- 실제 사용자 수요와 최신성·표본 수·탐색 성능의 장단점을 확인한 뒤 기본 동작 변경 또는 별도 `선택 지역 기록 더 불러오기` 제공 여부 결정

### RM-013 · [검토 필요] 다인큐 차단 목록 공유 · 서버 IP 직접 추가

- 고정 파티에서 문제 서버 IP를 공유할 수 있도록 `서버차단현황`의 보조 기능으로 서버 IP 직접 추가 또는 차단 목록 가져오기 제공 검토
- 차단은 파티 전체가 아니라 각 사용자 PC의 Windows 방화벽에 개별 적용된다는 점을 UI에 명확히 안내
- 정확한 공인 IPv4만 허용하고 사설·루프백·멀티캐스트·잘못된 주소는 거부
- 적용 전 IP 목록과 선택적 차단 사유를 다시 보여 주고, 여러 IP는 한 번의 관리자 권한 요청으로 처리
- 실제 다인큐 동작 검증과 사용자 수요를 확인한 뒤 세부 방식과 구현 시기 결정

## v1.0.0 예정

### RM-014 · [확정] 한국어·영어 언어 지원

- 프로그램 UI, 안내창과 오류 메시지의 영어 번역 및 한국어·영어 선택 기능 제공
- README, 개인정보 안내, 문제 해결 및 라이선스 안내의 영문 문서 제공
- 한국어·영어 기능과 릴리스 안내가 같은 내용을 유지하는지 배포 전 검증

## 보류 기록

아래 항목은 검토 이력을 보존하기 위한 기록이며 현재 작업 지시가 아닙니다.

### RM-006 · [보류] 투명한 빌드 출처 정보와 일관성 검증

**사용자 결정**

- 시험 구현한 Binary Build ID·Release Bundle ID·provenance와 패키지 내부 식별 일치 검사는 현재 제품·빌드·패키징에 적용하지 않음
- 당분간 정식 게시된 `v0.7.4`의 단순 패키징 방식과 공개 `SHA256SUMS.txt` 제공을 유지
- 구현 자료는 삭제하지 않고 [`deferred/release-integrity/README.md`](deferred/release-integrity/README.md)에 격리 보존

**재검토 조건**

- 복수 빌드 환경이나 외부 배포 담당자가 생겨 빌드 출처를 기계적으로 대조할 실제 필요가 확인된 경우에만 재검토

### RM-015 · [보류] Windows 공식 배포본 서명과 서명된 해시 매니페스트

**사용자 결정**

- Authenticode 코드 서명과 서명된 해시 매니페스트는 `v0.8.0` 및 현재 확정 배포 범위에 포함하지 않음
- 공식 릴리스 자산의 기존 SHA-256 목록은 계속 제공

**재검토 조건**

- 인증서 발급·보관·자동 서명 환경과 지속 비용을 감당할 배포 운영 체계가 마련되거나 실제 사용자 요구가 확인된 경우에 재검토

### RM-016 · [보류] 공개 Release 자산 재다운로드 검증 자동화

**사용자 결정**

- AI 검토와 자동 업로드를 사용하는 현재 배포 규모에서는 과도한 작업으로 판단해 필수 로드맵에서 제외
- 시험 구현이 있더라도 정식 배포 과정에 연결하지 않음
- 시험 구현은 [`deferred/release-integrity/README.md`](deferred/release-integrity/README.md)에 보존

**재검토 조건**

- 배포 담당자·배포 채널·릴리스 빈도가 늘거나 실제 자산 누락·교체 사고가 발생한 경우에만 재검토

### RM-017 · [보류] 최종 배포물 전용 민감정보·압축 안전 자동 검사

**사용자 결정**

- 현재 배포 흐름에서는 별도 전용 검사 체계를 두지 않고 AI 검토와 기존 자동 빌드 검증을 사용
- 시험 구현이 있더라도 정식 패키징의 필수 실패 조건으로 연결하지 않음
- 시험 구현은 [`deferred/release-integrity/README.md`](deferred/release-integrity/README.md)에 보존

**재검토 조건**

- 외부 기여자·복수 빌드 환경·서드파티 번들 파일이 증가하거나 실제 민감정보 포함 사고가 발생한 경우에만 재검토
