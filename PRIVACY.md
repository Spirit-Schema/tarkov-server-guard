# 개인정보 및 네트워크

Tarkov Server Guard는 게임 서버 정보를 사용자의 PC에서 분석합니다. 게임 로그를 수집하거나 외부 서비스에 제보하는 기능은 없습니다.

## 로컬에서 읽는 정보

- EFT·Arena 게임 로그: 레이드 시각, 서버 IP, 연결 상태, RTT, 패킷손실 등 화면 표시에 필요한 항목
- EFT 런처 로그: `selectedGame`과 서버 적용 이벤트
- Arena `Regions.ini`: 지역명, 선택 상태, 자동 선택 여부와 파일 수정 시각
- Steam `libraryfolders.vdf`와 EFT AppID `3932890`의 설치 manifest: 설치 경로 탐색에 필요한 항목

프로필 ID 같은 계정 식별자와 EFT 런처 로그의 인증·계정 필드는 해석하거나 저장하지 않습니다. 유저신고 로그에서는 성공 응답이 확인된 `/client/report/send` 요청의 시각과 횟수만 사용하며, 신고 대상 닉네임·ID·사유는 자동으로 읽지 않습니다.

## PC에 저장하는 정보

모든 항목은 `%LOCALAPPDATA%\TarkovServerGuard` 아래에 저장됩니다.

| 항목 | 저장 내용 |
| --- | --- |
| `RaidNotes` | 사용자가 작성한 레이드 메모, 태그와 연결한 스크린샷 경로 |
| `UserReportMemos` | 사용자가 작성한 유저신고 메모와 보관함 표시용 정보 |
| `blocked-server-metadata.json` | 차단 서버의 데이터센터·추정 지역·차단 시각과 사용자가 선택 입력한 차단 메모 |
| `usage-guide.shown` | 첫 실행 사용방법을 표시했는지 여부만 기록하는 빈 파일 |
| `DbIpLite` | 지역 조회용 DB-IP Lite 데이터와 직전 정상본 |

메모 파일명은 원본 SID나 경로가 아닌 SHA-256 기반 키를 사용합니다. 유저신고 메모에는 사용자가 입력한 내용과 보관함 표시에 필요한 게임·맵·레이드 시각·성공 신고 건수만 저장합니다.

스크린샷은 원본을 복사·삭제·업로드하지 않고 사용자가 선택한 로컬 파일 경로만 연결합니다. 게임 로그 원문·계정 ID·SID·로그 경로는 메모에 저장하지 않습니다.

## 외부 통신

### 현재 핑

`조회`를 누르면 화면에 보이는 게임 서버로 ICMP 요청을 직접 보냅니다. 무응답만으로 차단 상태를 판단하지 않습니다.

### 지역 DB

게임 서버 IP의 지역은 PC에 저장된 DB-IP Lite City로 조회하며, IP를 DB-IP.com이나 다른 외부 지역 조회 API에 전송하지 않습니다.

최초 준비와 월간 갱신 때 `download.db-ip.com`에서 압축 DB를 내려받습니다. 조회 시 새 월간 지역 DB가 있으면 자동으로 업데이트합니다. 약 60~70MB를 새 파일로 교체하는 방식이며 매달 저장 용량이 누적되지 않습니다. 일반 HTTPS 다운로드와 마찬가지로 사용자의 공인 네트워크 IP와 앱 User-Agent는 다운로드 서버에 보일 수 있지만 게임 서버 IP·게임 로그·계정 정보·SID·로컬 경로는 요청에 포함하지 않습니다.

DB는 다운로드·검증 후 원자적으로 교체합니다. 손상되거나 예상 형식이 아닌 파일은 활성화하지 않고 직전 정상본을 유지합니다.

### 앱 업데이트

설치형과 Velopack Portable 빌드는 앱 UI가 열린 뒤 하루에 한 번 이하로 GitHub의 `Spirit-Schema/tarkov-server-guard` 공식 Releases를 확인합니다. 일반 HTTPS 요청에서 사용자의 공인 네트워크 IP와 앱 요청 정보는 GitHub에 보일 수 있지만 게임 서버 IP·게임 로그·계정 정보·SID·로컬 경로는 포함하지 않습니다.

새 안정 버전이 있을 때만 `업데이트`와 `나중에`를 표시합니다. 사용자가 동의해야 다운로드·적용하며, `나중에`를 선택하면 같은 버전 알림을 24시간 미룹니다.

## 관리자 권한과 방화벽

- 로그 탐색, 메모, 핑·지역·방화벽 상태 조회에는 관리자 권한이 필요하지 않습니다.
- `차단` 또는 `해제`로 Windows 방화벽을 변경할 때만 관리자 권한을 요청합니다.
- 해제는 앱이 관리하는 이름, 방향, 동작, 프로토콜과 단일 원격 IP가 모두 일치하는 규칙만 삭제합니다.
- 기존 `EFT_ExcludeChinaHighPingServer_<IP>`와 현재 `TarkovServerGuard_Block_<IP>` 규칙을 인식합니다.

패킷 캡처, 게임 메모리 접근, 게임 파일 수정은 하지 않습니다.

DB-IP Lite City는 [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/) 조건으로 사용합니다. 전체 외부 구성요소 고지는 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)를 참고하세요.
