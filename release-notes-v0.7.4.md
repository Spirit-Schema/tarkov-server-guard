# Tarkov Server Guard v0.7.4

좁은 화면 사용성과 기록 필터·업데이트 확인 흐름을 개선한 안정 업데이트입니다. 기존 메모·차단 정보·지역 DB는 그대로 유지됩니다.

## 변경 사항

- 창 최소 폭을 낮추고, 1180px 미만에서는 선택 서버의 고급 정보를 자동으로 숨겨 세로형 FHD 화면에서도 사용할 수 있습니다.
- 좁은 창에서는 작전시간·서버배정·연결 결과·버전·포트·shortId·SID를 자동으로 숨겨 핵심 정보 공간을 확보합니다.
- 좁은 폭에서 기간·메모·차단현황·필터 도구가 자연스럽게 두 줄로 배치됩니다.
- 최근 100개 기록에서 한국·일본·중국·싱가포르·말레이시아 등 원하는 데이터센터 지역과 `PVE로컬/기타`를 복수 선택할 수 있습니다.
- 기록 표를 가로로 스크롤해도 기존 디자인의 `차단`·`해제` 버튼 열은 오른쪽에 고정되어 바로 사용할 수 있습니다.
- `조회` 후 새로 반영된 로그 수와 100개 제한으로 제외된 오래된 기록 수를 안내하고, 현재 필터에 표시되는 최신 새 로그로 자동 이동합니다.
- 로그의 게임 시작·종료 기록으로 `작전시간`을 표시하고, 재접속 횟수는 서버연결 결과에 함께 표시합니다.
- 새 안정 버전 자동 확인 간격을 6시간으로 조정하고, 버전 옆 `업데이트확인`으로 즉시 확인할 수 있습니다.
- 우측 버전·저작권 영역을 두 줄 우측 정렬하고 라이선스 버튼 하단 잘림을 수정했습니다.

## 다운로드

- **일반 사용자 권장:** `SpiritSchema.TarkovServerGuard-win-Setup.exe`
- **설치 없이 사용:** `SpiritSchema.TarkovServerGuard-win-Portable.zip`

v0.7.3 사용자는 앱의 업데이트 안내 또는 `업데이트확인`을 통해 v0.7.4를 적용할 수 있습니다. Portable은 압축을 푼 뒤 폴더 안의 파일 구성을 유지해 주세요.

자세한 기능·사용법·개인정보·라이선스 안내는 [README](https://github.com/Spirit-Schema/tarkov-server-guard#readme)에서 확인할 수 있습니다.
