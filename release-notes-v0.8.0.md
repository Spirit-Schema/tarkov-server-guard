# Tarkov Server Guard v0.8.0

차단 목록 이식과 접속 기록 정확도, 메모 관리, 차단 상태 안내를 개선한 안정 업데이트입니다. 기존 메모·차단 정보·지역 DB는 그대로 유지됩니다.

## 변경 사항

- 서버차단현황에서 차단 목록을 파일로 내보내고, 새 PC나 포맷 후 선택 미리보기를 거쳐 다시 불러올 수 있습니다.
- 앱을 닫아도 Windows 방화벽에 저장된 차단 리스트가 재부팅 뒤까지 유지된다는 점을 서버차단현황에서 명확히 안내합니다.
- 앱 실행 후 `조회`를 누르기 전에도 현재 기록의 차단 상태를 읽기 전용으로 확인해 `현재 핑` 열에 `차단 중`을 우선 표시합니다.
- 로그의 `PvpSeason1`, `PvpSeason2`처럼 명시된 숫자를 클라이언트 버전과 관계없이 `PvP시즌1`, `PvP시즌2`로 `맵 · 게임유형` 열에 표시합니다.
- `GameStarted`의 `real` 값을 우선 활용하고 레이드 경계·재접속·중복 기록을 재검증해 입장시간 정확도를 높였습니다.
- 실게임 RTT·패킷손실 헤더를 정확한 두 줄로 표시하고 좁은 창에서도 불필요하게 넓어지지 않도록 조정했습니다.
- 메모보관함 헤더를 서버차단현황과 같은 디자인으로 통일하고, 체크한 일반 레이드·유저신고 메모를 한 번에 삭제할 수 있습니다.
- 유저신고 메모가 일반 레이드 메모와 함께 메모보관함에 저장된다는 안내로 수정했습니다.
- 업데이트가 실제로 완료된 설치본의 다음 첫 실행에서만 이번 패치 내역을 한 번 표시합니다.
- 패치 내역 본문의 흰색 네이티브 테두리를 다크 테마에 맞는 테두리로 교체했습니다.

## 다운로드

- **일반 사용자 권장:** `SpiritSchema.TarkovServerGuard-win-Setup.exe`
- **설치 없이 사용:** `SpiritSchema.TarkovServerGuard-win-Portable.zip`

v0.7.4 사용자는 앱의 업데이트 안내 또는 `업데이트확인`을 통해 v0.8.0을 적용할 수 있습니다. Portable은 압축을 푼 뒤 폴더 안의 파일 구성을 유지해 주세요.

자세한 기능·사용법·개인정보·라이선스 안내는 [README](https://github.com/Spirit-Schema/tarkov-server-guard#readme)에서 확인할 수 있습니다.
