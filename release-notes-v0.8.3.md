# Tarkov Server Guard v0.8.3

레이드 기록의 캐릭터·참가 형태와 서버 차단 판단 정보를 더 분명하게 확인할 수 있도록 확장한 업데이트입니다. 기존 메모·차단 정보·백업 형식·지역 DB는 그대로 유지됩니다.

## 변경 사항

- EFT의 `맵 · 게임유형` 표시에 로그로 확인된 `PMC` 또는 `스캐브`와 `솔로` 또는 `파티`를 순서대로 함께 표시합니다.
- 파티 알림의 최종 준비 상태가 온전하면 본인을 포함한 인원수를 `2인`부터 `5인`으로 표시하고, 인원수가 확인되지 않으면 `파티`로 표시합니다.
- PvP, PvE와 번호가 명시된 현재·향후 PvP 시즌에 같은 판정 구조를 사용하며, `PvpSeason1`, `PvpSeason2` 같은 로그 번호는 클라이언트 버전 매핑보다 우선합니다.
- ProfileId 관계와 파티의 실제 캐릭터 정보가 충돌하거나 로그가 불완전하면 PMC·스캐브·인원수를 추측하지 않고 확인되지 않은 부분만 생략합니다.
- 로컬 PvE는 `/client/match/local/start` 로그로 `솔로`를 확인하지만, 현재 확인된 로컬 로그에는 캐릭터를 직접 판정할 근거가 없어 PMC·스캐브 부분을 표시하지 않습니다.
- 새로 작성하는 일반 레이드 메모와 유저신고 메모에도 캐릭터와 참가 형태 표시를 보존해 메모보관함에서 함께 확인할 수 있습니다.
- 판정에 사용한 ProfileId·파티원 식별값·닉네임·장비 원문은 세션 캐시나 메모에 저장하지 않습니다.
- 서버를 차단하면 최근 레이드 최대 100개 중 해당 IP 사용 횟수와 RTT 150ms 이상·패킷손실 5% 이상·시간초과 중 하나 이상의 징후가 확인된 횟수를 기존 완료 상태에 표시합니다. 이는 로그에서 함께 관찰된 징후이며 해당 서버가 문제를 유발했다는 인과 판정이 아닙니다.
- 실게임 통계 행이 없거나 수신 표본이 0건으로 확인되면 RTT와 패킷손실을 모두 `로그없음`으로 표시합니다. 수신 표본이 1건 이상이면 별도의 최소 표본 기준 없이 유효한 로그 값을 표시합니다.
- 서버차단현황을 열 때 전체 로그에서 최신 레이드 최대 100개를 다시 확인해 차단 IP별 최근 실게임 RTT와 패킷손실을 표시합니다. 두 값은 각각 가장 최근의 유효값이므로 서로 다른 레이드에서 기록됐을 수 있으며, `최근 기록 없음`·`로그없음`·`확인 불가`를 구분합니다.
- 차단 규칙이 ICMP를 포함한 해당 IP 통신을 막으므로 차단 중 현재 핑은 `차단 중 · 측정 불가`로 표시합니다. 측정을 위해 차단을 자동으로 해제하지 않으며, 직접 해제한 뒤 메인 화면에서 조회할 수 있습니다.
- 차단 리스트는 이 PC에만 적용됩니다. 파티원 모두의 접속을 막으려면 각 파티원이 같은 서버를 차단해야 합니다.
- 사용방법 2번의 `나가기 확인` 뒤에 `(장비는 보존됩니다)` 안내를 같은 줄에 추가했습니다.

## 다운로드

- **일반 사용자 권장:** `SpiritSchema.TarkovServerGuard-win-Setup.exe`
- **설치 없이 사용:** `SpiritSchema.TarkovServerGuard-win-Portable.zip`

현재 공개된 v0.8.0 사용자는 앱의 업데이트 안내 또는 `업데이트확인`을 통해 v0.8.3을 적용할 수 있습니다. Portable은 압축을 푼 뒤 폴더 안의 파일 구성을 유지해 주세요.

자세한 기능·사용법·개인정보·라이선스 안내는 [README](https://github.com/Spirit-Schema/tarkov-server-guard#readme)에서 확인할 수 있습니다.
