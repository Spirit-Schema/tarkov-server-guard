# 문제 해결

## 로그 경로를 찾지 못합니다

앱은 공홈과 Steam 설치 위치를 확인한 뒤 실제 로그가 가장 최근인 EFT·Arena 경로를 선택합니다.

1. 설치 위치를 옮겼다면 `자동 찾기`를 누릅니다.
2. 계속 찾지 못하면 해당 게임의 `직접선택`에서 설치 폴더 또는 `Logs` 폴더를 선택합니다.
3. 경로를 바꾼 뒤 강조된 `적용`을 눌러 해당 경로를 저장하고 로그를 읽습니다.

Steam 탐지는 공식 EFT AppID `3932890`의 manifest를 사용합니다. Steam 계정·인증 파일은 읽지 않습니다.

## 원하는 레이드가 보이지 않습니다

- 기간과 `전체`·`EFT`·`Arena` 필터를 확인하세요.
- 기간 검색은 로그를 다시 읽은 뒤 중복을 제거하고 최신순 최대 100개를 표시합니다.
- 새 로그를 읽으려면 `조회`를 누르세요. 최신 로그를 다시 읽은 뒤 현재 목록의 서버 상태를 조회합니다. 자동 5초 갱신은 사용하지 않습니다.
- 로컬 PvE에는 연결할 게임 서버가 없어 서버 IP와 핑이 표시되지 않을 수 있습니다.

## 현재 핑이 `응답 없음`입니다

일부 서버는 ICMP 요청에 응답하지 않을 수 있습니다. 응답 없음이 곧 앱에서 차단했다는 뜻은 아닙니다. `차단 중`은 앱이 관리하는 Windows 방화벽 규칙을 직접 확인했을 때만 표시합니다.

## 지역이 실제 위치와 다릅니다

DB-IP Lite City는 무료 추정 데이터라 도시가 누락되거나 실제 데이터센터 위치와 다를 수 있습니다. 게임 서버 IP를 외부 지역 조회 API로 전송하지 않는 대신 정확도에는 한계가 있습니다.

지역 DB는 `%LOCALAPPDATA%\TarkovServerGuard\DbIpLite`에 저장됩니다. 조회 시 새 월간 지역 DB가 있으면 약 60~70MB를 새 파일로 교체하며 매달 누적하지 않습니다. 해제 DB는 약 130MB이고 직전 정상본을 함께 보관하면 약 260MB를 사용합니다. 갱신 중 임시 파일 때문에 최대 약 500MB의 여유 공간을 권장합니다.

다운로드·검증·교체가 실패하면 현재 정상 DB를 계속 사용하므로 앱을 다시 실행한 뒤 다음 조회에서 재시도할 수 있습니다.

## 차단 또는 해제가 되지 않습니다

- 방화벽 변경 시 표시되는 Windows 관리자 권한 요청을 승인했는지 확인하세요.
- `조회` 후 각 행의 현재 상태에 맞는 버튼만 활성화됩니다. 비활성 버튼을 누르면 사용할 수 없는 이유가 표시됩니다.
- `서버차단현황`에서는 로그를 삭제했더라도 앱의 현재 규칙을 확인하고 개별·선택·전체 해제할 수 있습니다.

앱은 현재 `TarkovServerGuard_Block_<IP>`와 이전 v1.1.1의 `EFT_ExcludeChinaHighPingServer_<IP>` 형식을 모두 인식합니다.

## 차단 후 게임에 접속실패가 표시됩니다

이미 배정된 서버의 새 아웃바운드 연결을 막으면 로딩이 멈추거나 `Server Connection Lost` 같은 접속실패가 표시될 수 있습니다. 앱의 `사용방법` 안내에 따라 레이드에서 나간 뒤 다시 매칭하세요. 차단한 레이드에 재접속하기 위한 기능은 아닙니다.

Arena 서버는 차단 시 탈주 페널티가 적용되는지 확인되지 않았습니다. 따라서 Arena에서는 전용 경고창의 붉은색 `차단`을 직접 선택한 경우에만 진행됩니다.

## Windows SmartScreen 안내가 표시됩니다

현재 실행 파일에는 코드 서명이 없어 `알 수 없는 게시자`로 표시될 수 있습니다. 반드시 공식 GitHub Releases에서 받고, 함께 제공하는 `SHA256SUMS.txt`와 파일의 SHA-256을 비교하세요.

```powershell
Get-FileHash -Algorithm SHA256 .\SpiritSchema.TarkovServerGuard-win-Setup.exe
```

## 자동 업데이트에 실패합니다

네트워크나 GitHub Releases 확인에 실패하면 현재 설치본을 그대로 유지하고 다른 기능은 계속 사용할 수 있습니다. 자동 업데이트는 공식 안정 버전만 대상으로 하며 사전 릴리스·같은 버전·이전 버전은 적용하지 않습니다.

개발용 단일 EXE에는 Velopack 런타임이 없어 자동 업데이트가 비활성화될 수 있습니다. 실제 배포용 Setup 또는 Portable을 사용하세요.

## 메모가 목록에서 사라졌습니다

메모는 게임 로그와 별도로 `%LOCALAPPDATA%\TarkovServerGuard\RaidNotes`와 `UserReportMemos`에 보관됩니다. 최근 100개 목록에서 레이드가 사라지거나 원본 로그를 삭제해도 `메모 보관함`에서 일반 레이드 메모와 유저신고 메모를 확인할 수 있습니다.

첨부 스크린샷은 파일 자체가 아니라 경로만 저장하므로 원본을 이동하거나 삭제하면 앱에서 열 수 없습니다.
